- item = OpenStruct.new(item)
.item
  .entry
    .pic
      %a(href='#' data-id="#{item.num_iid}")
        %img(src="#{item.pic_url}" title="#{strip_tags(item.title)}")
    .desc
      .pull-right= "#{item.volume}件/30天"
      %span= item.commission
      \/
      %span.price= item.price
